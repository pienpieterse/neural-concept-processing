
```bash
# From project root
uv sync
.venv/Scripts/activate
```

to run the RSA scripts, e.g.
```bash
# From project root
uv run -m scripts.RSA1and2
```

to the the results:
run the results/clean_up_results.ipynb notebook